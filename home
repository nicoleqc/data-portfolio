<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Nicole Quispe — Impact Data Strategist</title>
    <style>
        :root {
            --bg: #ffffff;
            --text: #1a1a1a;
            --accent: #1e40af; /* Azul profundo y profesional */
            --muted: #333333;
            --font: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            --wpp: #25D366;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: var(--font);
            background-color: var(--bg);
            color: var(--text);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 4rem 2rem;
        }

        header {
            display: flex;
            align-items: center;
            gap: 3rem;
            margin-bottom: 5rem;
        }

        .profile-img {
            width: 180px;
            height: 180px;
            border-radius: 12px;
            object-fit: cover;
            flex-shrink: 0;
        }

        h1 { font-size: 2.5rem; font-weight: 800; margin-bottom: 0.5rem; color: var(--text); }
        .subtitle { font-size: 1.15rem; color: var(--muted); max-width: 600px; }

        section { margin-bottom: 6rem; }
        h2 { font-size: 1.75rem; margin-bottom: 3rem; font-weight: 700; border-bottom: 2px solid #f1f5f9; padding-bottom: 0.5rem; }

        .project-card {
            display: grid;
            grid-template-columns: 1fr 1.2fr;
            gap: 4rem;
            margin-bottom: 6rem;
            align-items: start;
        }

        .project-info h3 { font-size: 1.5rem; margin-bottom: 1rem; color: var(--text); }
        .project-info p { margin-bottom: 1.5rem; color: var(--muted); }

        .destacables { background: #f8fafc; padding: 1.5rem; border-radius: 12px; margin-bottom: 1.5rem; }
        .destacables ul { list-style: none; }
        .destacables li::before { content: "→"; color: var(--accent); margin-right: 0.5rem; font-weight: bold; }

        .project-link { 
            display: inline-block; 
            color: var(--accent); 
            text-decoration: none; 
            font-weight: 600; 
            border-bottom: 2px solid transparent;
            transition: all 0.2s;
        }
        .project-link:hover { border-bottom-color: var(--accent); }

        .tag { 
            display: inline-block; 
            background: #eff6ff; 
            color: var(--accent); 
            padding: 0.25rem 0.75rem; 
            border-radius: 99px; 
            font-size: 0.8rem; 
            font-weight: 600;
            margin-right: 0.5rem;
        }

        /* Gráfico de Gantt minimalista */
        .gantt-chart { margin-top: 2rem; }
        .gantt-row { display: flex; align-items: center; margin-bottom: 1.25rem; gap: 1rem; }
        .gantt-label { width: 150px; font-size: 0.85rem; font-weight: 600; }
        .gantt-bar-bg { flex-grow: 1; background: #f1f5f9; height: 12px; border-radius: 6px; position: relative; }
        .gantt-bar { height: 100%; border-radius: 6px; background: var(--accent); position: absolute; }
        
        .bar-w1 { width: 25%; left: 0; background: #60a5fa; }
        .bar-w2 { width: 25%; left: 25%; background: #3b82f6; }
        .bar-w3 { width: 40%; left: 50%; background: #2563eb; }
        .bar-w4 { width: 10%; left: 90%; background: #1e40af; }

        .wpp-link { 
            background: var(--wpp); color: white; padding: 0.8rem 2rem; border-radius: 8px; 
            text-decoration: none; font-weight: 700; display: inline-block; margin-top: 1rem;
        }

        @media (max-width: 850px) {
            .project-card { grid-template-columns: 1fr; gap: 2rem; }
            header { flex-direction: column; text-align: center; }
            .contacto-layout { flex-direction: column; }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <!-- Sustituye con tu foto -->
            <img src="perfil.jpg" alt="Nicole Quispe" class="profile-img">
            <div class="header-content">
                <h1>Nicole Quispe</h1>
                <p class="subtitle">Consultora de Estrategia de Datos con más de 3 años de experiencia en el sector bancario y de consultoría tecnológica. He liderado proyectos de análisis de datos para proyectos de Desarrollo Sostenible para Latinoamérica.</p>
            </div>
        </header>

        <section id="proyectos">
            <h2>Proyectos Seleccionados</h2>

            <!-- Proyecto 1 -->
            <div class="project-card">
                <div class="project-info">
                    <h3>Impact Data Analytics</h3>
                    <p>Modelo predictivo de riesgo de exclusión laboral femenina desarrollado para PNUD y ONU Mujeres.</p>
                    <div class="destacables">
                        <ul>
                            <li>Liderazgo de equipo multidisciplinario.</li>
                            <li>Integración de IA para predicción de brechas.</li>
                            <li>Dashboard interactivo en Power BI.</li>
                        </ul>
                    </div>
                    <a href="#" class="project-link">Ver detalles →</a>
                </div>
                <div class="project-visuals">
                    <img src="proyecto1.png" alt="Visualización Dashboard" style="width:100%; border-radius:8px;">
                </div>
            </div>
        </section>

        <section id="contacto">
            <h2>¿Tienes un reto de datos?</h2>
            <p>Conversemos sobre cómo transformar tu información en impacto real.</p>
            <a href="https://wa.me/51972169858" target="_blank" class="wpp-link">Escribir a WhatsApp</a>
            
            <div class="gantt-chart">
                <div class="gantt-row">
                    <div class="gantt-label">Análisis & Discovery</div>
                    <div class="gantt-bar-bg"><div class="gantt-bar bar-w1"></div></div>
                </div>
                <div class="gantt-row">
                    <div class="gantt-label">Release & MVP</div>
                    <div class="gantt-bar-bg"><div class="gantt-bar bar-w4"></div></div>
                </div>
            </div>
        </section>

        <footer>
            &copy; 2026 Nicole Quispe · LinkedIn
        </footer>
    </div>
</body>
</html>
